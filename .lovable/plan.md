\n# Plan: Sistema de compra con PayPal, validación IA, Telegram y entrega por email\n\n## Resumen del flujo\n\n```\nUsuario → elige plan → ve aviso → paga en PayPal.me → sube comprobante + email\n   ↓\nIA (Lovable AI - Gemini) valida imagen (PayPal real, monto, receptor)\n   ↓ válido\nTelegram bot envía al admin con botones [Aprobar] [Rechazar] [Ver Usuario]\n   ↓ admin aprueba\nSistema toma key disponible del inventario → marca usada → envía por email (Lovable Emails)\n   ↓ admin rechaza\nNotifica al usuario, permite reenvío\n```\n\n## 1. Cambios en base de datos\n\nNueva tabla `payment_orders`:\n- `id`, `email`, `plan_duration` ('1 día' | '7 días' | '30 días')\n- `amount_usd` (4, 7 o 15)\n- `receipt_url` (storage)\n- `status` ('PENDING' | 'AI_REJECTED' | 'APPROVED' | 'REJECTED')\n- `ai_validation` (jsonb con resultado IA)\n- `telegram_message_id`, `assigned_key`, `email_sent_attempts`, `email_sent_at`\n- `rejection_reason`, `created_at`, `updated_at`\n\nBucket de storage `payment-receipts` (público para que IA y admin puedan ver).\n\nRLS: insert público (anyone puede crear orden), select público por email/id, update solo via service role (edge functions).\n\n## 2. Frontend (Login → BuyKey)\n\nReemplazar UI de Square por nuevo flujo en `src/pages/BuyKey.tsx`:\n- 3 planes: 1 día/$4, 7 días/$7, 30 días/$15\n- Al elegir plan: modal con aviso obligatorio + botón \